
import express, { Router } from "express";

const router = Router();

// create project
router.post("/",)

// get all projects
router.get("/",)

// delete project
router.delete("/:projectId",)

export default router;